# ── Core numerical / vision ──────────────────────────────────────────────────
numpy>=1.24
opencv-python>=4.8
pyyaml>=6.0

# ── PyTorch (GPU-accelerated) ─────────────────────────────────────────────────
# Install the CUDA variant that matches your driver.
# Example (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU-only fallback (much slower for SuperPoint/LightGlue):
#   pip install torch torchvision
torch>=2.1
torchvision>=0.16

# ── Feature extraction / matching ─────────────────────────────────────────────
# LightGlue is not on PyPI; install directly from the GitHub repo:
#   pip install git+https://github.com/cvg/LightGlue.git
# The line below is a placeholder for documentation purposes.
# lightglue @ git+https://github.com/cvg/LightGlue.git
